FROM node:20
WORKDIR /app

# Install deps first (better layer caching)
COPY package*.json ./
RUN npm ci --only=production=false

# Copy source code
COPY . .

EXPOSE 3000

# Run Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]